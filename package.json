{
  "name": "website",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "cd packages/common && yarn start",
    "build": "lerna exec --scope @clearlydefined/ui-components -- babel src -d dist --ignore test.js,story.js --copy-files"
  }
}

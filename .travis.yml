language: node_js
# node version specified in .nvmrc
before_script:
  - sh -c 'cd ./components && npm ci && npm pack && cd - && npm install $PWD/components/clearlydefined-ui-components-*.tgz'
cache:
  directories:
    - "node_modules"
